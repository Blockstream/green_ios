#include "ga_strings.hpp"

namespace ga {
namespace sdk {
    namespace res {

        const std::string fee_rate_below_minimum{ "Fee rate is below minimum accepted fee rate" };
        const std::string inconsistent_gauth_data{ "Inconsistent data provided for enabling gauth" };
        const std::string insufficient_funds{ "Insufficient funds" };
        const std::string no_outputs{ "No outputs" };
        const std::string send_all_requires_single{ "Send all requires a single output" };

    } // namespace res
} // namespace sdk
} // namespace ga
